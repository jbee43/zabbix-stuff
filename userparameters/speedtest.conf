# Reads the cached JSON written by speedtest-stats.sh and triggers a
# background refresh on every poll (a full speedtest exceeds the 30s agent
# Timeout limit, so it cannot run synchronously). flock inside the read
# script prevents overlapping runs.
UserParameter=speedtest.stats,/etc/zabbix/scripts/speedtest-read.sh
